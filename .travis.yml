sudo: require
language: cpp

compiler:
  - g++

before_install:
  - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
  - sudo apt-get update -qq
  - sudo pip install codecov

install:

script:
  - ./build.sh

after_success:
  - codecov
